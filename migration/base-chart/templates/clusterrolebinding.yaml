{{- if and .Values.rbac.create .Values.rbac.clusterRole.create }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "base-chart.fullname" . }}-cluster-role-binding
  labels:
    {{- include "base-chart.labels" . | nindent 4 }}
  annotations:
    {{- with .Values.defaultAnnotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "base-chart.fullname" . }}-cluster-role
subjects:
  - kind: ServiceAccount
    name: {{ include "base-chart.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
{{- end }}
