{{- if .Values.configMapEnvVars }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "base-chart.fullname" . }}-configMapEnvVars
  annotations:
    viafoura.co/{{ include "base-chart.fullname" . }}-configMapEnvVars-checksum: {{ toYaml .Values.configMapEnvVars | sha256sum }}
  {{- if .Values.defaultAnnotations }}
    {{- with .Values.defaultAnnotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- end }}
data:
{{- with .Values.configMapEnvVars }}
  {{- toYaml . | nindent 2 }}
{{- end }}
{{- end }}
