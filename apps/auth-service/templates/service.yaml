---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "auth-service.serviceName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "auth-service.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- include "auth-service.servicePorts" . | nindent 4 }}
  selector:
    {{- include "auth-service.selectorLabels" . | nindent 4 }}
