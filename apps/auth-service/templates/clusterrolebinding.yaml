{{- if and .Values.rbac.create .Values.rbac.clusterRole.create }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "auth-service.fullname" . }}-cluster-role-binding
  labels:
    {{- include "auth-service.labels" . | nindent 4 }}
  annotations:
    {{- with .Values.defaultAnnotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "auth-service.fullname" . }}-cluster-role
subjects:
  - kind: ServiceAccount
    name: {{ include "auth-service.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
{{- end }}
