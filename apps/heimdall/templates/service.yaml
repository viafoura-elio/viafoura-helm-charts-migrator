---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "heimdall.serviceName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "heimdall.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- include "heimdall.servicePorts" . | nindent 4 }}
  selector:
    {{- include "heimdall.selectorLabels" . | nindent 4 }}
