{{- if and .Values.rbac.create .Values.rbac.role.create }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "tyrion.fullname" . }}-role-binding
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "tyrion.labels" . | nindent 4 }}
  {{- if .Values.defaultAnnotations }}
  annotations:
    {{- with .Values.defaultAnnotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- end }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "tyrion.fullname" . }}-role
subjects:
  - kind: ServiceAccount
    name: {{ include "tyrion.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
{{- end }}
