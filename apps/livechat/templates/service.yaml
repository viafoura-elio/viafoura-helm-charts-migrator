---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "livechat.serviceName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "livechat.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- include "livechat.servicePorts" . | nindent 4 }}
  selector:
    {{- include "livechat.selectorLabels" . | nindent 4 }}