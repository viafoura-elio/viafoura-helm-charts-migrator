{{- if .Values.serviceAccount.create -}}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "livecomments.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "livecomments.labels" . | nindent 4 }}
    {{- if .Values.serviceAccount.podIdentity.enabled }}
    # Pod Identity labels
    app.kubernetes.io/name: {{ include "livecomments.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    {{- end }}
    {{- if .Values.rbac.create }}
    # RBAC labels for security auditing
    security.kubernetes.io/rbac-enabled: "true"
    {{- end }}
  {{- if or .Values.rbac.create
     .Values.serviceAccount.annotations
     .Values.defaultAnnotations
  }}
  annotations:
    {{- if .Values.rbac.create }}
    # RBAC annotations for security compliance
    security.kubernetes.io/least-privilege: "true"
    security.kubernetes.io/rbac-role: {{ include "livecomments.roleName" . | quote }}
    {{- if .Values.rbac.clusterRole.create }}
    security.kubernetes.io/rbac-cluster-role: {{ include "livecomments.clusterRoleName" . | quote }}
    {{- end }}
    {{- end }}
    {{- with .Values.serviceAccount.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
    {{- if .Values.serviceAccount.iamRole.enabled }}
    # IRSA annotation
    eks.amazonaws.com/role-arn: {{ include "livecomments.iamRoleArn" . }}
    {{- end }}
    {{- if .Values.serviceAccount.podIdentity.enabled }}
    # Pod Identity annotation
    eks.amazonaws.com/pod-identity-association: {{ include "livecomments.podIdentityAssociationName" . }}
    {{- end }}
    {{- with .Values.defaultAnnotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- end }}
automountServiceAccountToken: {{ .Values.serviceAccount.automount }}
{{- end }}
